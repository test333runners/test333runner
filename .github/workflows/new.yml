name: Testing
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  runs-on: [self-hosted]
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: uname -a
